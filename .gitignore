# ── Compiled objects ──
*.o
*.a

# ── Local build (name without arch suffix) ──
rastertolh
rastertolhplh
rastertolhplh-dbg
rastertolhplh.arm

# ── Local ARM object files (cross-compile artifacts) ──
jbig85-*.o
jbig_ar-*.o

# ── JBIG-KIT (downloaded by install.sh) ──
jbigkit-2.1/
jbigkit-2.1.tar.gz

# ── Test / debug files ──
test_output.raw
test_raster.raw
test.ps
test_*.raw
test_*.txt

# ── OS / editor ──
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
.idea/

# ── Packaging ──
*.deb
*.7z
*.tar.gz
*.zip

# Windows 官方驱动二进制（闭源，GPLv2-only 用于互操作性研究）
analysis/windows-driver/*.dll
analysis/windows-driver/*.exe
analysis/windows-driver/*.SYS
analysis/windows-driver/*.INF

# 临时构建产物 / 调试文件
*.bak
*.bak*
rastertolhplh_dbg
rastertolhplh-*-dbg
